# Copy to .env and fill in your values
# cp .env.example .env

# Gemini API (for visual QA via API mode, not needed for --use-cli)
GOOGLE_API_KEY=

# Anthropic API (for code review/fix via API mode, not needed for --use-cli)
ANTHROPIC_API_KEY=
